<main>
  <h2>商品一覧</h2>

  <% @items.each do |item| %>

    <%= link_to item_path(item.id) do %>
    <%= image_tag item.get_image %>
    <% end %>

    <%= link_to item_path(item.id) do %>
    <%= item.name %>
    <% end %>

    <%= item.price %>

 <% end %>

</main>