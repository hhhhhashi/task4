<main>
  <p>ようこそ、ながのCAKEへ！</br>
  このサイトは、ケーキ販売のECサイトになります。</br>
  会員でない方も商品の閲覧はできますが、</br>
  購入には会員登録が必要になります。</p>

  <h2 class="mb-4">新着商品</h2>

  <div class="container">
    <div class="row">
      <% @items.each do |item| %>
        <div class="mr-auto">
          <div>
            <%= link_to item_path(item.id) do %>
            <%= image_tag item.get_profile_image(200,200) %>
            <% end %>
          </div>

          <div>
            <%= link_to item_path(item.id) do %>
            <%= item.name %>
            <% end %>
          </div>

          <div>
            ¥<%= item.price %>
          </div>
        </div>
      <% end %>
    </div>
  </div>

 <%= paginate @items,class: "mt-4" %>

</main>